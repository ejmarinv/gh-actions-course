name: 03 - Workflow Runners

on: 
    push


jobs: 
     ubuntu-echo:
          runs-on: ubuntu-latest
          steps:
            - name: Show OS
              run: |
                "This job is running on an Ubuntu runner."
                "Runner OS: $RUNNER_OS"

